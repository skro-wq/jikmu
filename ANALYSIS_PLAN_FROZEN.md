# 분석계획 (사전 동결)

**동결 시각:** 2026-07-24 04:0x KST
**동결 대상 실행:** `outputs/sim_results_v10_gpt5.csv` (N=512, GPT-5, seed 20250807)
**동결 사유:** 설계를 v1→v10까지 열 차례 개정하는 동안 중간 결과를 관찰하였다. 어떤 검정을
주검정으로 삼을지 실행 전에 확정하지 않으면, 어떤 결과를 보고하더라도 사후 선택으로 읽힌다.
이 문서는 실행 **전에** 작성되었으며 실행 후 수정하지 않는다.

---

## 1. 주검정 (하나)

```
서열 로짓 (proportional odds):
    강도 ~ 형식수준 + 보상비율수준 + 인지모드(S1 더미)

강도: 수용 0 < 협상 1 < 청구 2 < 소송 3
관심 계수: 형식수준. 양측 alpha = .05.
H1 지지 조건: beta_형식 < 0 이고 p < .05.
```

**서열 DV를 쓰는 이유 (실행 전 근거).** 기존 이진 DV(수용 대 나머지)는 과소보상 부표본에서
포화된다. v3 정본 실측: 형식별 분쟁률 100 / 97.8 / 100 / 95.2% (n=191, chi2 p=.211).
같은 부표본에서 법적행동률은 51.9 / 37.0 / 39.2 / 19.0% (beta=-0.417, **p=.003**).
신호는 "분쟁하느냐"가 아니라 "얼마나 세게 가느냐"에 있다. 이진 DV는 그 신호를 버린다.

## 2. 보조 검정 (결과와 무관하게 전부 보고)

1. 이진 분쟁 DV: 형식 × 분쟁 교차분석, 카이제곱·Cramer's V·Spearman
2. 법적행동(청구+소송) 이진 로짓, 동일 예측변수
3. H3: 인지모드 × 강도. 주검정 모형의 S1 계수로 보고
4. 보상비율 수준별 형식 효과 (기술 통계로만. 셀당 n=128이므로 p값 보고하지 않음)
5. 충격유형별 강도 분포 (기술 통계로만)

## 3. 탐색적 (그렇게 명시)

산업·발명중요도·감정동인별 분해, 추론 기록의 정성 분석.

## 4. 보고 의무

- **전체 표본만 보고한다.** 정당보상·격차·규모에 의한 절단 분석은 하지 않는다.
  (근거: v3에서 절단 임계값별 p가 .009 ~ .888로 비단조 진동하였다. 어떤 임계값도
  사후 정당화 가능하므로 전면 금지한다.)
- 매니페스트(`*_manifest.json`)의 모델·백엔드·호출형태·시드·재시도·드롭을 본문에 기재한다.
- 설계 개정 이력과 각 개정을 촉발한 중간 관찰을 한계 절에 전부 공개한다. 특히
  "정당보상 100만원 절단 시 H1이 p=.009에서 p=.301로 변한 것을 관찰한 뒤 층화 설계를
  채택하였다"는 사실을 명시한다.
- 감정은 검정 대상이 아니다. 매개분석·인과 서술을 하지 않는다.
- H2의 상대적 예측력 주장은 하지 않는다. 격차와 충격배율은 생성식으로 연결되어 있다.
  (v10 설계에서 Spearman -0.001까지 분리되었으나, 주장은 계수 부호와 유의성에 한정한다.)

## 5. 사전 등록된 예상

v10 스모크(n=64) 기준 형식 beta = -0.896 (se 0.429, p=.037). n=512에서 표준오차는
약 0.152로 줄어들 것이므로, 효과가 실재한다면 |z| ~ 5.9. 만약 n=512에서 유의하지 않게
나오면 **그 귀무 결과를 그대로 보고한다.** 추가 설계 개정이나 부표본 탐색을 하지 않는다.

## 6. 실패 조건 (이 경우 결과를 폐기하고 재설계를 공개한다)

- 파싱 실패·드롭이 표본의 2%를 넘는 경우
- 32셀 중 어느 셀이라도 목표치(16건)에 미달하는 경우
- 전체 분쟁률이 5% 미만 또는 95% 초과인 경우
